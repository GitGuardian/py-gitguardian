<!--
A new scriv changelog fragment.

Uncomment the section that is right (remove the HTML comment wrapper).
-->

<!--
### Removed

- A bullet item for the Removed category.

-->

### Added

- New endpoint `log_mcp_activities_bulk()`: send a batch of historical `MCPActivityRequest` to GitGuardian and receive ingested/duplicate counts. Adds `MCPActivityBulkResponse` model.

- Optional `timestamp` and `idempotency_key` fields on `MCPActivityRequest`, used by the bulk endpoint to record historical events idempotently. Live (single-event) `log_mcp_activity` callers are unaffected; both new fields default to `None`.

<!--
### Changed

- A bullet item for the Changed category.

-->
<!--
### Deprecated

- A bullet item for the Deprecated category.

-->
<!--
### Fixed

- A bullet item for the Fixed category.

-->
<!--
### Security

- A bullet item for the Security category.

-->
